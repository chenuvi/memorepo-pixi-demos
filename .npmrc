# 设置 Node 版本
node-version=18.x

# 启用工作区
enable-workspace=true

# 安装对等依赖
auto-install-peers=true

# 强制使用确切版本
save-exact=true

# Netlify 和 Vercel 会使用这个命令安装 pnpm
shamefully-hoist=true

# 启用 pnpm 提升模式，确保构建环境兼容性
public-hoist-pattern[]=*

# 跳过预构建步骤
ignore-scripts=false
prefer-frozen-lockfile=false